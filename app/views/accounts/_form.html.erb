<%= form_with(model: account, local: true) do |form| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :account_name %>
  </div>

  <div class="field">
    <%= form.label :name_long %>
    <%= form.text_field :name_long, id: :account_name_long %>
  </div>

  <div class="field">
    <%= form.label :is_service %>
    <%= form.check_box :is_service, id: :account_is_service %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password, id: :account_password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
