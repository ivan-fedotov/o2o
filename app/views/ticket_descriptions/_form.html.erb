<%= form_with(model: ticket_description, local: true) do |form| %>
  <% if ticket_description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_description.errors.count, "error") %> prohibited this ticket_description from being saved:</h2>

      <ul>
      <% ticket_description.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :ticket_description_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
